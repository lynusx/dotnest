# DotNest UI 重新设计文档

## 设计概览

本次 UI 重新设计采用了现代化的设计语言，提升了视觉层次和用户体验，同时保持了所有现有的输入输出逻辑不变。

## 设计特点

### 1. 配色方案

#### 主色调
- **Primary**: `#6366F1` (紫蓝色) - 现代、科技感
- **Primary Light**: `#818CF8` - 渐变使用
- **Primary Dark**: `#4F46E5` - 深色强调
- **Primary Container**: `#EEF2FF` - 浅色背景

#### 侧边栏
- **背景**: `#1E1E2E` (深色) - 提供对比度
- **选中项**: `#2D2D3F` - 微妙的高亮
- **悬停**: `#252535` - 交互反馈
- **指示器**: `#818CF8` - 与主色调呼应

#### 状态颜色
- **成功**: `#10B981` (绿色)
- **错误**: `#EF4444` (红色)
- **警告**: `#F59E0B` (橙色)
- **信息**: `#3B82F6` (蓝色)

### 2. 设计元素

#### 圆角
- 卡片: `16px`
- 按钮: `12px`
- 输入框: `10px`
- 标签: `6-8px`

#### 阴影
- **shadowSm**: 轻微阴影，用于小元素
- **shadowMd**: 中等阴影，用于卡片
- **shadowLg**: 大阴影，用于浮动元素

#### 渐变
- 页面背景使用微妙的渐变
- 按钮和图标使用主色调渐变
- 提供深度感和现代感

### 3. 组件设计

#### 侧边栏
- **宽度**: 240px
- **深色主题**: 提供视觉对比
- **圆润菜单项**: 12px 圆角
- **悬停动画**: 200ms 缓动
- **选中状态**: 发光圆点指示器
- **品牌展示**: 顶部显示 Logo 和应用名称

#### 页面头部
- **渐变图标容器**: 主色调渐变 + 阴影
- **双层文字**: 标题 + 描述
- **操作按钮**: 右侧对齐
- **分隔线**: 底部边框

#### 配置面板
- **固定宽度**: 380px
- **卡片样式**: 白色背景 + 边框 + 阴影
- **内边距**: 24px
- **标题图标**: 带背景色的圆润图标
- **悬停效果**: 输入框边框颜色变化

#### 结果面板
- **自适应宽度**: 使用 Expanded
- **卡片容器**: 统一的卡片样式
- **统计栏**: 彩色标签展示数据
- **列表项**: 悬停时背景色变化

#### 按钮
- **主按钮**: 主色调 + 白色文字
- **次要按钮**: 边框按钮
- **圆角**: 12px
- **内边距**: 16px 垂直
- **加载状态**: 圆形进度指示器

#### 输入框
- **背景色**: 内容区背景色
- **边框**: 浅灰色
- **聚焦**: 主色调边框 (2px)
- **圆角**: 10px
- **内边距**: 12-14px

#### 空状态
- **圆形图标容器**: 主色调背景
- **居中布局**: 垂直居中
- **双层文字**: 标题 + 说明

### 4. 动画效果

- **悬停动画**: 200ms 缓动曲线
- **选中动画**: 150ms
- **页面切换**: 200ms 淡入淡出
- **加载指示器**: 循环旋转

### 5. 响应式设计

- 使用 `flutter_screenutil` 适配不同屏幕尺寸
- 设计基准: 1280x800
- 文字、间距、圆角均使用响应式单位

## 已完成的页面

### 1. 主布局 (MainLayout)
- ✅ 现代化深色侧边栏
- ✅ 平滑的菜单项动画
- ✅ 渐变分隔线
- ✅ 品牌展示区域

### 2. API 提取页面 (ExtractPage)
- ✅ 渐变背景
- ✅ 页面头部设计
- ✅ 配置面板重构
- ✅ 悬停效果的输入框
- ✅ 彩色统计卡片
- ✅ 列表项悬停动画

### 3. 设置页面 (SettingsPage)
- ✅ 卡片式布局
- ✅ 字体大小滑块
- ✅ 语雀配置区域
- ✅ 信息提示框

### 4. 链接重写页面 (LinkRewritePage)
- ✅ 与 ExtractPage 一致的设计语言
- ✅ SegmentedButton 输出模式选择
- ✅ 彩色统计展示

### 5. 知识库列表页面 (RepoListPage)
- ✅ 现代化表格设计
- ✅ 悬停行高亮
- ✅ 点击复制功能
- ✅ 彩色标签显示 ID

### 6. 主题系统 (AppTheme)
- ✅ 完整的颜色系统
- ✅ 预定义阴影
- ✅ Material 3 主题配置
- ✅ 统一的组件样式

## 待完成的页面

以下页面结构较为复杂，可以根据已完成页面的设计模式进行更新：

1. **文档列表页面** (doc_list_page.dart)
2. **目录更新页面** (toc_update_page.dart)
3. **批量上传页面** (batch_upload_page.dart)

这些页面的更新可以复用以下设计元素：
- 页面头部布局
- 配置面板样式
- 输入框和按钮样式
- 统计和结果展示

## 设计原则

1. **一致性**: 所有页面使用统一的设计语言
2. **可访问性**: 足够的颜色对比度，清晰的视觉层次
3. **响应式**: 适配不同屏幕尺寸
4. **现代化**: 使用渐变、阴影、圆角等现代设计元素
5. **交互反馈**: 悬停、点击等状态有明确的视觉反馈
6. **逻辑不变**: 所有输入输出逻辑保持不变

## 技术实现

- **框架**: Flutter 3.x + Material 3
- **状态管理**: Provider
- **响应式**: flutter_screenutil
- **架构**: MVVM (View 层重构，ViewModel 保持不变)

## 视觉效果

新设计提供了：
- 更好的视觉层次
- 更清晰的信息架构
- 更流畅的交互体验
- 更现代的视觉风格
- 更统一的设计语言

## 维护建议

1. 新页面应遵循相同的设计规范
2. 颜色值使用 `AppColors` 常量
3. 阴影使用预定义的 `shadowSm/Md/Lg`
4. 圆角尺寸保持一致性
5. 动画时长使用 150-200ms
